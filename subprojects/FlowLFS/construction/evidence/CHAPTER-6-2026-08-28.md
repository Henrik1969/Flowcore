# Chapter 6 construction evidence — 2026-08-28

The temporary cross-compiled tools from LFS r13.0-201-systemd Chapter 6 were
built in the sealed builder VM against the Chapter 5 target checkpoint.

## Observed result

- All 17 durable package markers were present.
- The construction log ended with `CHAPTER 6 COMPLETE 2026-08-28T21:02:59Z`.
- Target GCC identified itself as GCC 16.2.0.
- Target Bash identified itself as Bash 5.3.0 for `x86_64-lfs-linux-gnu`.
- `/bin/sh` resolved to `bash` and `/usr/bin/cc` resolved to `gcc`.
- `/usr/lib64` was absent, as required by the temporary-system layout.
- Target filesystem use was 4.4 GiB of the 40 GiB virtual disk.
- The builder shut down normally and unmounted `/mnt/lfs` cleanly.

The target-side log and markers remain within the checkpoint at
`/var/log/flowlfs/ch06`. The immutable checkpoint digest is recorded in
`ch6-target.sha256`.

Running a target dynamic executable directly from the builder namespace is
not a valid runtime test because its loader searches the builder filesystem;
target-native execution begins in the Chapter 7 chroot boundary.
