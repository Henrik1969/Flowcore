# Chapter 7 construction evidence — 2026-08-28

The LFS r13.0-201-systemd Chapter 7 temporary target-native environment was
built inside the target chroot from the locally pinned canonical archives.

## Observed result

- Filesystem/identity setup, all eight packages, and cleanup produced markers.
- The log ended with `CHAPTER 7 COMPLETE 2026-08-28T21:12:31Z`.
- Target-native versions included GCC 16.2.0, Perl 5.44.0, Python 3.14.7,
  Bison 3.8.2, and Util-linux 2.42.2.
- `/tools` was absent after the validated cleanup boundary.
- The virtual kernel filesystems were explicitly unmounted before shutdown.
- Target filesystem use was 3.1 GiB of the 40 GiB virtual disk.
- `qemu-img check` reported no errors before sealing the checkpoint.

The first chroot entry attempt stopped before package execution because its
diagnostic used `findmnt`, which is not available until this chapter builds
Util-linux. The diagnostic was corrected to use `/proc/self/mounts`; the
resumable construction then completed. Logs and markers remain inside the
checkpoint at `/var/log/flowlfs/ch07`.
