# Chapters 2–4 evidence — 2026-08-28

## Result

PASS. The dedicated VM target is partitioned, mounted, populated with exactly
the authorized sources, and prepared with the clean LFS build identity. No
package compilation has started.

## Pre-write target proof

Immediately before partitioning, `/dev/vdb` was a 42,949,672,960-byte block
device of type `disk` with no child partition, filesystem signature, or mount.
The preparation helper independently repeated those checks and required the
literal confirmation `--confirm-destroy=/dev/vdb`.

## Partition and filesystem

```text
table:       MBR / dos
disk:        /dev/vdb
partition:   /dev/vdb1
start:       sector 2048 (1 MiB)
size:        83,884,032 sectors
type:        83, bootable
filesystem:  ext4
label:       FLOWLFS_ROOT
UUID:        0becbbb1-b058-4eab-acbc-c8cedeae355b
mount:       /mnt/lfs, rw,relatime
```

The installer OS remains on `/dev/vda2`. The repository attachment was mounted
read-only via 9p at `/mnt/flowlfs-project`.

## Authorized inputs

- Checksum-manifest entries: 94
- Payload files copied: 94
- Target verification: 94/94 OK
- Staged source bytes: 658,035,501
- Cache extras copied: 0
- `$LFS/sources` mode: sticky and world-writable as required by Chapter 3
- Payload ownership after verification: root:root

## Chapter 4 state

- Required `/etc`, `/var`, `/usr/{bin,lib,sbin}`, `/tools`, and `/lib64`
  directories exist.
- `/bin`, `/lib`, and `/sbin` are relative links into `/usr`.
- `/usr/lib64` is absent.
- User/group `lfs` exists as UID/GID 1001 in the disposable builder.
- Chapter-build directories are owned by `lfs` as required.
- Builder `/etc/bash.bashrc` was moved to `/etc/bash.bashrc.NOUSE` in the
  disposable working overlay.

Validated clean environment:

```text
LFS=/mnt/lfs
LFS_TGT=x86_64-lfs-linux-gnu
LC_ALL=POSIX
PATH=/mnt/lfs/tools/bin:/usr/bin
CONFIG_SITE=/mnt/lfs/usr/share/config.site
MAKEFLAGS=-j4
umask=0022
```

## Reversible checkpoint

After a clean guest shutdown, both QCOW2 images passed `qemu-img check`.
The Chapter 4 target was sealed read-only as
`flowlfs-target-ch4-ready.qcow2`, and the active target is now an empty
disposable overlay backed by it.

Sealed Chapter 4 target SHA-256:
`679863d1a6c42575713176e20645c0bbf9f0ea7457bbcae3cd9e5ade7bf01fd3`.
