# flowforge.cargo-package.v0.2

Version 0.2 adds private build `HOME`, `TMPDIR`, Cargo home, and neutral Git
configuration. It also admits declarative `generate` rows for documentation
and completions produced by the compiled executable. No shell evaluation is
permitted.
