---
title: Flowmini v0.24 Accepted-Language Coverage Matrix
status: active-completion-checklist
kind: current-state-audit
---

# Flowmini v0.24 Accepted-Language Coverage Matrix

## Purpose and authority

This matrix audits syntax accepted by the executable `parseModule()` frontend
against the explicit raw AST and structural SymbolTable path. Its dimensions
and exit rule are binding for v0.24. Row statuses describe this candidate
checkout: published merge `8f1c9fc` plus the locally reviewed C5 working-tree
changes. They must evolve with implementation evidence.

A green runtime suite does not prove raw-AST coverage. An AST-only probe does
not prove coverage of syntax accepted by `parseModule()`.

Every canonical construct must have canonical syntax representation, stable
ownership/IDs where cross-reference is required, source provenance, its
intended SymbolTable projection, and focused golden coverage.

## Syntax classification

| Class | Effect on v0.24 gate |
|---|---|
| **Canonical** | Counts against raw-AST and structural-projection completion |
| **Compatibility-only** | Remains on the `parseModule()`/`ModuleSpec` path and does not force Graph IR concepts into the raw AST |
| **Provisional** | Accepted or probed experimentally but cannot define the canonical model until decided |
| **Historical** | Retained as provenance; not an active language requirement |

Legacy direct node, wire, and policy syntax is compatibility-only. Canonical
nodes, ports, wires, and policies belong to Graph IR after the semantic frontend
border, not to v0.24 raw AST.

`print value` is provisional compatibility syntax. Printing is an ordinary
call through a capability/provider function such as `print(value)` or later
`io.print(value)`; it must not create `StatementKind::Print`.

Placement is canonical source meaning. `expr -> ordinary_target` is a
`PlacementStatement` owning a value expression and an `AssignableTarget`.
`expr -> return` is a `ReturnStatement` owning `value_expression`, with
source-form provenance recording arrow placement syntax. Return is control
transfer, not storage, and must never be represented as a fake assignable
target. AST-only `name = expr` and `return expr` probes do not satisfy this
accepted-syntax coverage.

For v0.24, `AssignableTarget` matches currently accepted executable syntax
exactly and is a tagged variant of `IdentifierTarget`, `FieldPathTarget`, or
`IndexedTarget`. It is intentionally not a generic expression target. Future
extensions add explicit target variants rather than making every expression
assignable. Scope, mutability, type compatibility, and assignability are later
semantic-analysis decisions.

The initial payload contract mirrors `parseAssignableTarget()` exactly:

```text
IdentifierTarget
    name
    source location

FieldPathTarget
    base identifier
    ordered field segments, each with source location

IndexedTarget
    base identifier
    one or more index ExpressionIds
```

Index components reference ordinary expressions, but the target remains a
closed l-value-shaped structure.

Statements preserve their source spelling as provenance without changing
semantic ownership:

```text
PlacementStatement
    source_form = ArrowPlacement

ReturnStatement
    source_form = KeywordReturn | ArrowPlacement

AssignmentStatement
    source_form = EqualsAssignment
```

Assignment, placement, and return remain distinct statement forms. Both return
spellings denote control transfer; neither makes `return` an assignable target.

## C5 typed statement direction

`Statement` owns location plus a typed payload variant. Statement kind is
derived from payload type; each payload owns only fields meaningful to that
form:

```text
Statement::Payload = variant<
    LetStatement,
    AssignmentStatement,
    PlacementStatement,
    IfStatement,
    WhileStatement,
    BreakStatement,
    ContinueStatement,
    ReturnStatement,
    ExpressionStatement,
    FlowStatement,
    UnknownStatement
>
```

Generic kind strings, expression-ID arrays, and legacy JSON fields are derived
compatibility projections, never competing semantic state.

Migration checkpoints:

```text
C5a  define typed statement payload structs
C5b  derive StatementKind and JSON compatibility projections
C5c  migrate Let, Assignment, and Return ownership
C5d  migrate If, While, ElseArm, and BlockId ownership
C5e  implement and migrate PlacementStatement
C5f  remove irrelevant optional scaffolding after golden coverage
```

Current implementation checkpoint:

```text
C5a  implemented: all typed payload and AssignableTarget structures exist
C5b  implemented: StatementKind and JSON compatibility fields are derived
C5c  implemented: Let, Assignment, and keyword Return own their data in payloads
C5d  implemented: If/While own conditions and blocks; If owns ElseArm
C5e  implemented: arrow placement and arrow-return preserve exact source form
C5f  implemented: obsolete shared optional statement fields are removed
```

The golden validator checks exact payload shapes and payload/projection
agreement for all emitted statement forms. C5 completion is an implementation
checkpoint, not closure of the v0.24 frontend gate.

| Mark | Meaning |
|---|---|
| **C** | Complete for the accepted form and guarded by evidence |
| **P** | Partial; accepted information is missing |
| **A** | Absent from this layer |
| **N/A** | This dimension does not apply |

## Source units and top-level declarations

| Construct | Class | AST | IDs | Provenance | Symbols | Goldens | Evidence or gap |
|---|---|---:|---:|---:|---:|---:|---|
| `program name` | Canonical | C | P | C | C | C | Source unit is explicit; top-level declarations are not arena-owned. |
| `unit name` | Canonical | C | P | C | C | P | Modeled, but needs focused unit AST/projection coverage. |
| legacy `module name` | Historical/compatibility | A | A | A | A | A | Accepted by `parseModule()`; not part of the canonical v0.24 gate. |
| root `main { ... }` | Canonical | C | C | C | C | C | Main owns a `BlockId`; projection creates a main scope. |
| internal `fn` | Canonical | P | P | C | P | P | Signature/body shell exists; accepted body surface is incomplete. |
| record `type` block | Canonical | P | P | C | P | P | Record/fields exist; contract and projection coverage is narrow. |
| refined type with invariants | Canonical | P | P | P | P | A | Reduced to alias/base; invariant operators and values disappear. |
| `abi` block | Canonical | A | A | A | A | A | Library, convention, and nested declarations are absent. |
| ABI pointer-shaped type contract | Canonical | A | A | A | A | A | Ownership/access/lifetime/nullability facts are absent. |
| ABI struct | Canonical | A | A | A | A | A | Identity and field contracts are absent. |
| ABI extern function | Canonical | A | A | A | A | A | Library, symbol, effect, arguments, and return contract are absent. |
| legacy producer/node/sink | Compatibility-only | A | A | A | A | A | Remains on the direct `ModuleSpec` path. |
| legacy wire declaration/chain | Compatibility-only | A | A | A | A | A | Canonical flow topology belongs to Graph IR. |
| legacy policy declaration | Compatibility-only | A | A | A | A | A | Canonical policy representation belongs after the frontend border. |
| qualified node declaration/policies | Compatibility-only | A | A | A | A | A | Generic runtime sugar lowers directly to `ModuleSpec`. |

## Statements, blocks, and targets

| Construct | Class | AST | IDs | Provenance | Symbols | Goldens | Evidence or gap |
|---|---|---:|---:|---:|---:|---:|---|
| braced block | Canonical | C | C | C | C | C | Arena-owned block with ordered statement IDs. |
| typed declaration with initializer | Canonical | P | C | C | P | C | Core let/type/value exists; type-specific contracts may disappear. |
| int/Bool/refined-int/c_string declarations | Canonical | P | C | C | P | P | Unified let shell loses some accepted distinctions. |
| `list<int>` declaration | Canonical | P | C | C | P | P | Type/literal exist; initializer constraints are not structural facts. |
| shaped `array<int>` declaration | Canonical | P | C | C | P | P | Flattened initializer/shape contract is not guaranteed. |
| record value declaration | Canonical | P | C | C | P | P | Declared-record contract link is unresolved. |
| ABI struct value declaration | Canonical | A | A | A | A | A | Blocked on the missing ABI model. |
| `expr -> identifier` placement | Canonical | C | C | C | N/A | C | `PlacementStatement` owns value, located identifier target, and arrow source form. |
| placement to field/index | Canonical | C | C | C | N/A | C | Field path preserves ordered located segments; indexed target owns one or more expression IDs. |
| `expr -> return` | Canonical | C | C | C | N/A | C | `ReturnStatement(value_expression, ArrowPlacement)`; return is not an assignable target. |
| `print expr` | Provisional compatibility | A | A | A | N/A | A | Must migrate toward an ordinary capability/provider call, never `StatementKind::Print`. |
| `if` with braced body | Canonical | C | C | C | C | C | Condition, then block, ownership, and projection are guarded. |
| ordinary `else` | Canonical | C | C | C | C | C | Explicit `ElseBlock(BlockId)`. |
| `else if` | Canonical | C | C | C | C | C | Direct `ElseIf(IfStmtId)` continuation. |
| `while` | Canonical | C | C | C | C | C | Condition/body are arena-owned and projected. |
| `break` / `continue` | Canonical | C | C | C | N/A | P | Present; focused ownership/nesting goldens are still needed. |
| plain `name = expr` | Provisional AST probe | C | C | C | N/A | C | Distinct `AssignmentStatement` with `EqualsAssignment`; does not cover placement. |
| `return expr` | Provisional AST probe | C | C | C | N/A | C | `ReturnStatement` with `KeywordReturn`; does not cover arrow provenance. |

## Expressions and assignable targets

| Construct | Class | AST | IDs | Provenance | Symbols | Goldens | Evidence or gap |
|---|---|---:|---:|---:|---:|---:|---|
| integer/Boolean/string literal | Canonical | C | C | C | N/A | C | Canonical payloads and expression IDs. |
| identifier use | Canonical | C | C | C | A | C | Raw use exists; semantic resolution is later. |
| unary `not` and prefix minus | Canonical | P | C | C | N/A | P | Accepted spellings need focused coverage. |
| arithmetic/comparison expression | Canonical | C | C | C | N/A | C | Precedence and recursive children are guarded. |
| parenthesized expression | Canonical | C | C | C | N/A | C | Grouping controls shape without a synthetic node. |
| function call | Canonical | C | C | C | A | C | Callee/arguments exist; callable resolution is later. |
| `stdin.int()` / `length(list)` | Provisional intrinsic spelling | P | C | C | N/A | P | Generic call/access shape does not preserve explicit capability role. |
| list/array indexing | Canonical | P | C | C | N/A | C | Container kind/rank is unresolved. |
| field-access chain | Canonical | P | C | C | N/A | C | Chain provenance coverage needs strengthening. |
| list/record literal | Canonical | C | C | C | N/A | C | Elements and named fields have canonical value IDs. |
| assignable identifier/field/index target | Canonical | C | C | C | N/A | C | Closed v0.24 variants and payload/projection validation are covered by the placement probe. |

## Type and contract surface

| Construct | Class | AST | IDs | Provenance | Symbols | Goldens | Evidence or gap |
|---|---|---:|---:|---:|---:|---:|---|
| named/generic type reference | Canonical | C | P | C | P | C | Type nodes are values rather than arena entities. |
| array type/extents | Canonical | C | P | C | P | C | Element type and ordered extent locations exist. |
| refined invariant | Canonical | A | A | A | A | A | Operator, literal, order, and location must be preserved. |
| record field contract | Canonical | P | P | C | P | P | Stable declaration links are missing. |
| ABI ownership/lifetime/access | Canonical | A | A | A | A | A | Entire accepted ABI contract surface remains to be modeled. |
| extern effect contract | Canonical | A | A | A | A | A | Accepted by `parseModule()` but absent from explicit AST. |

## Structural SymbolTable checkpoint

The current two-probe projection covers source-unit, import, function,
parameter, record, field, alias, main, block, and local-let symbols for AST
forms that exist. It proves the bridge, not full accepted-language coverage.

Completion requires stable links to AST declarations/source locations and all
intended declaration/scope rows above. Resolution, type correctness, derived
facts, and contract satisfaction remain semantic-analysis work.

## v0.24 exit rule

v0.24 completes only when every `parseModule()` form is classified; every
canonical accepted form is represented losslessly; compatibility-only forms
remain isolated from the canonical AST contract; provisional forms have an
explicit canonical/compatibility/historical decision; syntax cannot disappear into skipped tokens or
unknown placeholders; IDs, parentage, references, and provenance validate; the
intended structural projection is complete; focused goldens cover every row;
all normal gates remain green; and current documentation matches the checkout.

Only then may the line advance to v0.25 SymbolTable maturation and later v0.26
semantic analysis.
