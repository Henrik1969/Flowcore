program bad_abi_type_missing_contract

abi bad {
    library "libc.so.6"
    convention c

    type c_bad_pointer {
        repr "void*"
        ownership borrowed
        access read
    }
}

main {
    x : int(0)
    print x
}
