unit unit_symbol_projection_gallery

type ExternalAlias refines vendor.Domain

type Packet {
    field payload : optional<collection.list<int>>
}

fn convert(input : Bool) -> float {
    return input
}
