unit std_collatz

fn collatz_next(n : int): int {
    zero  : int(0)
    two   : int(2)
    three : int(3)
    one   : int(1)
    rem   : int(0)

    n % two -> rem

    if rem == zero {
        n / two -> return
    } else {
        n * three + one -> return
    }
}
