import "../../std/abi/kernel.flow" as linux

program arbitrary_external_branch

main {
    parent : c_int(0)
    linux.getppid() -> parent
    if parent > 0 {
        return 42
    } else {
        return 7
    }
}
