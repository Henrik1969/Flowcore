import "../../std/abi/kernel.flow"

program abi_kernel_random_main

main {
    buffer : c_pointer(0)
    count : c_size_t(1)
    flags : c_int(0)
    result : c_long(0)
    getrandom(buffer, count, flags) -> result
}
