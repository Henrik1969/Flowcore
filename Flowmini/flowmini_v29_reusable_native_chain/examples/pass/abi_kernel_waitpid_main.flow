import "../../std/abi/kernel.flow"
program abi_kernel_waitpid_main
main {
    process : c_int(-1)
    status : c_pointer(0)
    options : c_int(1)
    result : c_int(0)
    waitpid(process, status, options) -> result
}
