import "../../std/abi/kernel.flow"
program abi_kernel_connect_main
main {
    descriptor : c_int(-1)
    address : c_pointer(0)
    length : c_size_t(0)
    result : c_int(0)
    connect(descriptor, address, length) -> result
}
