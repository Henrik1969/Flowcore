import "../../std/abi/kernel.flow"

program abi_kernel_unlinkat_main

main {
    directory : c_int(-1)
    path : c_string("/flowcore-nonexistent")
    flags : c_int(0)
    result : c_int(0)
    unlinkat(directory, path, flags) -> result
}
