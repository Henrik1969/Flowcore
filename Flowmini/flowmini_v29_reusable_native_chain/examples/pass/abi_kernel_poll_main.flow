import "../../std/abi/kernel.flow"
program abi_kernel_poll_main
main {
    fds : c_pointer(0)
    count : c_size_t(0)
    timeout : c_int(0)
    result : c_int(0)
    poll(fds, count, timeout) -> result
}
