import "../../std/abi/kernel.flow"
program abi_kernel_socket_main
main {
    domain : c_int(-1)
    kind : c_int(1)
    protocol : c_int(0)
    result : c_int(0)
    socket(domain, kind, protocol) -> result
}
