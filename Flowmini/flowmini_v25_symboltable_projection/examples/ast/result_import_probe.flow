import "../../std/result.flow"

program result_import_probe

enum ParseError { invalid }

main {
    result : Result<int,ParseError>(Result.ok(7))
    marker : int(0)
    when result {
        case Result.ok {
            value -> marker
        }
        default {
            -1 -> marker
        }
    }
}
