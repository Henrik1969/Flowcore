program expression_kind_probe

fn square(n : int) -> int {
    return n * n
}

main {
    a : int(1)
    b : int(2)
    c : float(3.14)
    s : string("hello")
    flag : Bool(true)

    a = b

    while a < 10 {
        a = a + 1
    }

    if flag {
        b = a
    }
}
