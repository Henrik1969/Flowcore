program utf8_reader_runtime_probe

main {
    bytes : list<int>(stdin.bytes())
    lead : int(0)
    continuation : int(0)
    scalar : int(0)

    bytes[1] -> lead
    bytes[2] -> continuation
    (lead - 192) * 64 + continuation - 128 -> scalar
    print scalar
}
