import "../../std/abi/kernel.flow"

program abi_kernel_read_main

main {
    descriptor : c_int(-1)
    buffer : c_pointer(0)
    count : c_size_t(1)
    result : c_long(0)
    read(descriptor, buffer, count) -> result
}
