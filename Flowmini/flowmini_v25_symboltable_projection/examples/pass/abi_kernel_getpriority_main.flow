import "../../std/abi/kernel.flow" as linux

program abi_kernel_getpriority_main

main {
    which : c_int(0)
    who : c_int(0)
    priority : c_int(0)
    linux.getpriority(which, who) -> priority
}
