import "../../std/abi/kernel.flow"
program abi_kernel_accept4_main
main {
    descriptor : c_int(-1)
    address : c_pointer(0)
    length : c_pointer(0)
    flags : c_int(0)
    result : c_int(0)
    accept4(descriptor, address, length, flags) -> result
}
