import "../../std/abi/kernel.flow"
program abi_kernel_socketpair_main
main {
    domain : c_int(1)
    kind : c_int(1)
    protocol : c_int(0)
    sockets : c_pointer(0)
    result : c_int(0)
    socketpair(domain, kind, protocol, sockets) -> result
}
