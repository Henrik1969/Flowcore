module bad_abi_unsupported_signature

abi badlibc {
    library "libc.so.6"
    convention c

    extern fn badstrlen(text : c_string): c_long {
        symbol "strlen"
        effect pure
    }
}

main {
    out : c_long(0)
    badstrlen("Hello") -> out
    print out
}
